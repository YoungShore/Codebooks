/* Paragraph came from Lang Chen's preparation on 12/21/2017 for RISE */

if find(description,'BETAMETHASONE', 'i') or find(description,'CELESTONE' ,'i') then GNN_steroid='BETAMETHASONE';
if find(description,'BUDESONIDE', 'i') or find(description,'BUDESONIDE' ,'i') then GNN_steroid='BUDESONIDE';
if find(description,'BUDESONIDE', 'i') or find(description,'ENTOCORT' ,'i') then GNN_steroid='BUDESONIDE';
if find(description,'BUDESONIDE', 'i') or find(description,'UCERIS' ,'i') then GNN_steroid='BUDESONIDE';
if find(description,'CORTISONE', 'i') or find(description,'CORTISONE' ,'i') then GNN_steroid='CORTISONE';
if find(description,'CORTISONE', 'i') or find(description,'CORTONE' ,'i') then GNN_steroid='CORTISONE';
if find(description,'CORTISONE', 'i') or find(description,'PANTISONE' ,'i') then GNN_steroid='CORTISONE';
if find(description,'DEXAMETHASONE', 'i') or find(description,'BAYCADRON' ,'i') then GNN_steroid='DEXAMETHASONE';
if find(description,'DEXAMETHASONE', 'i') or find(description,'CONCEDEX' ,'i') then GNN_steroid='DEXAMETHASONE';
if find(description,'DEXAMETHASONE', 'i') or find(description,'DECADRON' ,'i') then GNN_steroid='DEXAMETHASONE';
if find(description,'DEXAMETHASONE', 'i') or find(description,'DECAMETH' ,'i') then GNN_steroid='DEXAMETHASONE';
if find(description,'DEXAMETHASONE', 'i') or find(description,'DECAREX' ,'i') then GNN_steroid='DEXAMETHASONE';
if find(description,'DEXAMETHASONE', 'i') or find(description,'DEXAMETHASONE' ,'i') then GNN_steroid='DEXAMETHASONE';
if find(description,'DEXAMETHASONE', 'i') or find(description,'DEXAPACK' ,'i') then GNN_steroid='DEXAMETHASONE';
if find(description,'DEXAMETHASONE', 'i') or find(description,'DEXONE' ,'i') then GNN_steroid='DEXAMETHASONE';
if find(description,'DEXAMETHASONE', 'i') or find(description,'DEXPAK' ,'i') then GNN_steroid='DEXAMETHASONE';
if find(description,'DEXAMETHASONE', 'i') or find(description,'HEXADROL' ,'i') then GNN_steroid='DEXAMETHASONE';
if find(description,'DEXAMETHASONE', 'i') or find(description,'MYMETHASONE' ,'i') then GNN_steroid='DEXAMETHASONE';
if find(description,'DEXAMETHASONE', 'i') or find(description,'ZEMA-PAK' ,'i') then GNN_steroid='DEXAMETHASONE';
if find(description,'HYDROCORTISONE', 'i') or find(description,'CORTEF' ,'i') then GNN_steroid='HYDROCORTISONE';
if find(description,'HYDROCORTISONE', 'i') or find(description,'HYDROCORTISONE' ,'i') then GNN_steroid='HYDROCORTISONE';
if find(description,'HYDROCORTISONE', 'i') or find(description,'HYDROCORTONE' ,'i') then GNN_steroid='HYDROCORTISONE';
if find(description,'HYDROCORTISONE', 'i') or find(description,'PANHYDROSONE' ,'i') then GNN_steroid='HYDROCORTISONE';
if find(description,'PREDNISOLONE', 'i') or find(description,'ASMALPRED' ,'i') then GNN_steroid='PREDNISOLONE';
if find(description,'PREDNISOLONE', 'i') or find(description,'BUBBLI-PRED' ,'i') then GNN_steroid='PREDNISOLONE';
if find(description,'PREDNISOLONE', 'i') or find(description,'DELTA-CORTEF' ,'i') then GNN_steroid='PREDNISOLONE';
if find(description,'PREDNISOLONE', 'i') or find(description,'FERNISOLONE-P' ,'i') then GNN_steroid='PREDNISOLONE';
if find(description,'PREDNISOLONE', 'i') or find(description,'FLO-PRED' ,'i') then GNN_steroid='PREDNISOLONE';
if find(description,'PREDNISOLONE', 'i') or find(description,'MEDLONE' ,'i') then GNN_steroid='PREDNISOLONE';
if find(description,'PREDNISOLONE', 'i') or find(description,'MEDROL' ,'i') then GNN_steroid='PREDNISOLONE';
if find(description,'PREDNISOLONE', 'i') or find(description,'MEPROLONE' ,'i') then GNN_steroid='PREDNISOLONE';
if find(description,'PREDNISOLONE', 'i') or find(description,'METHYLPRED' ,'i') then GNN_steroid='PREDNISOLONE';
if find(description,'PREDNISOLONE', 'i') or find(description,'METHYLPREDNISOLONE' ,'i') then GNN_steroid='PREDNISOLONE';
if find(description,'PREDNISOLONE', 'i') or find(description,'METHYLPREDNISONE' ,'i') then GNN_steroid='PREDNISOLONE';
if find(description,'PREDNISOLONE', 'i') or find(description,'METROCORT' ,'i') then GNN_steroid='PREDNISOLONE';
if find(description,'PREDNISOLONE', 'i') or find(description,'MILLIPRED' ,'i') then GNN_steroid='PREDNISOLONE';
if find(description,'PREDNISOLONE', 'i') or find(description,'ORAPRED' ,'i') then GNN_steroid='PREDNISOLONE';
if find(description,'PREDNISOLONE', 'i') or find(description,'PEDIAPRED' ,'i') then GNN_steroid='PREDNISOLONE';
if find(description,'PREDNISOLONE', 'i') or find(description,'PREDNISOLONE' ,'i') then GNN_steroid='PREDNISOLONE';
if find(description,'PREDNISOLONE', 'i') or find(description,'PREDOXINE' ,'i') then GNN_steroid='PREDNISOLONE';
if find(description,'PREDNISOLONE', 'i') or find(description,'PRELONE' ,'i') then GNN_steroid='PREDNISOLONE';
if find(description,'PREDNISOLONE', 'i') or find(description,'STERANE' ,'i') then GNN_steroid='PREDNISOLONE';
if find(description,'PREDNISOLONE', 'i') or find(description,'ULACORT' ,'i') then GNN_steroid='PREDNISOLONE';
if find(description,'PREDNISOLONE', 'i') or find(description,'VERIPRED' ,'i') then GNN_steroid='PREDNISOLONE';
if find(description,'PREDNISONE', 'i') or find(description,'DELTASONE' ,'i') then GNN_steroid='PREDNISONE';
if find(description,'PREDNISONE', 'i') or find(description,'FERNISONE' ,'i') then GNN_steroid='PREDNISONE';
if find(description,'PREDNISONE', 'i') or find(description,'LIQUID PRED' ,'i') then GNN_steroid='PREDNISONE';
if find(description,'PREDNISONE', 'i') or find(description,'LISACORT' ,'i') then GNN_steroid='PREDNISONE';
if find(description,'PREDNISONE', 'i') or find(description,'METICORTEN' ,'i') then GNN_steroid='PREDNISONE';
if find(description,'PREDNISONE', 'i') or find(description,'ORASONE' ,'i') then GNN_steroid='PREDNISONE';
if find(description,'PREDNISONE', 'i') or find(description,'PAN-SONE' ,'i') then GNN_steroid='PREDNISONE';
if find(description,'PREDNISONE', 'i') or find(description,'PANASOL' ,'i') then GNN_steroid='PREDNISONE';
if find(description,'PREDNISONE', 'i') or find(description,'PREDNICEN-M' ,'i') then GNN_steroid='PREDNISONE';
if find(description,'PREDNISONE', 'i') or find(description,'PREDNISONE' ,'i') then GNN_steroid='PREDNISONE';
if find(description,'PREDNISONE', 'i') or find(description,'PREDNISONE-5' ,'i') then GNN_steroid='PREDNISONE';
if find(description,'PREDNISONE', 'i') or find(description,'PRONAX' ,'i') then GNN_steroid='PREDNISONE';
if find(description,'PREDNISONE', 'i') or find(description,'RAYOS' ,'i') then GNN_steroid='PREDNISONE';
if find(description,'PREDNISONE', 'i') or find(description,'STERAPRED' ,'i') then GNN_steroid='PREDNISONE';
if find(description,'PREDNISONE', 'i') or find(description,'STERAPRED DS' ,'i') then GNN_steroid='PREDNISONE';
if find(description,'TRIAMCINOLONE', 'i') or find(description,'ARISTO-PAK' ,'i') then GNN_steroid='TRIAMCINOLONE';
if find(description,'TRIAMCINOLONE', 'i') or find(description,'ARISTOCORT' ,'i') then GNN_steroid='TRIAMCINOLONE';
if find(description,'TRIAMCINOLONE', 'i') or find(description,'KENACORT' ,'i') then GNN_steroid='TRIAMCINOLONE';
if find(description,'TRIAMCINOLONE', 'i') or find(description,'TRIAMCINOLONE' ,'i') then GNN_steroid='TRIAMCINOLONE';
if find(description,'TRIAMCINOLONE', 'i') or find(description,'TRIAMCINOLONE ACETONIDE' ,'i') then GNN_steroid='TRIAMCINOLONE';
