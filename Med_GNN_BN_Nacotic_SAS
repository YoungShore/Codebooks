/* Paragraph came from Lang Chen's preparation on 12/21/2017 for RISE project */

if find(description,'BUPRENORPHINE', 'i') or find(description,'BUTRANS' ,'i') then GNN_steroid='BUPRENORPHINE';
if find(description,'CODEINE', 'i') or find(description,'AN-ABATE' ,'i') then GNN_steroid='CODEINE';
if find(description,'CODEINE', 'i') or find(description,'BUTALB-ACETAMINOPH-CAFF-CODEIN' ,'i') then GNN_steroid='CODEINE';
if find(description,'CODEINE', 'i') or find(description,'BUTALB-CAFF-ACETAMINOPH-CODEIN' ,'i') then GNN_steroid='CODEINE';
if find(description,'CODEINE', 'i') or find(description,'COASTALDYNE' ,'i') then GNN_steroid='CODEINE';
if find(description,'CODEINE', 'i') or find(description,'COCET' ,'i') then GNN_steroid='CODEINE';
if find(description,'CODEINE', 'i') or find(description,'COCET PLUS' ,'i') then GNN_steroid='CODEINE';
if find(description,'CODEINE', 'i') or find(description,'CODACA' ,'i') then GNN_steroid='CODEINE';
if find(description,'CODEINE', 'i') or find(description,'CODAPHEN' ,'i') then GNN_steroid='CODEINE';
if find(description,'CODEINE', 'i') or find(description,'CODEGESIC' ,'i') then GNN_steroid='CODEINE';
if find(description,'CODEINE', 'i') or find(description,'COPAN' ,'i') then GNN_steroid='CODEINE';
if find(description,'CODEINE', 'i') or find(description,'EMCODEINE' ,'i') then GNN_steroid='CODEINE';
if find(description,'CODEINE', 'i') or find(description,'M-GESIC' ,'i') then GNN_steroid='CODEINE';
if find(description,'CODEINE', 'i') or find(description,'MI-CODE' ,'i') then GNN_steroid='CODEINE';
if find(description,'CODEINE', 'i') or find(description,'PAPA-DEINE NO.4' ,'i') then GNN_steroid='CODEINE';
if find(description,'CODEINE', 'i') or find(description,'PROVAL NO.3' ,'i') then GNN_steroid='CODEINE';
if find(description,'CODEINE', 'i') or find(description,'STOPAYNE' ,'i') then GNN_steroid='CODEINE';
if find(description,'CODEINE', 'i') or find(description,'TYLAGESIC 3' ,'i') then GNN_steroid='CODEINE';
if find(description,'CODEINE', 'i') or find(description,'UMI-APAP' ,'i') then GNN_steroid='CODEINE';
if find(description,'CODEINE', 'i') or find(description,'VITAPAP W/CODEINE' ,'i') then GNN_steroid='CODEINE';
if find(description,'CODEINE', 'i') or find(description,'VOPAC' ,'i') then GNN_steroid='CODEINE';
if find(description,'DIHYDROCODEINE', 'i') or find(description,'ACETAMINOPH-CAFF-DIHYDROCODEIN' ,'i') then GNN_steroid='DIHYDROCODEINE';
if find(description,'DIHYDROCODEINE', 'i') or find(description,'ASPIRIN-CAFFEINE-DIHYDROCODEIN' ,'i') then GNN_steroid='DIHYDROCODEINE';
if find(description,'DIHYDROCODEINE', 'i') or find(description,'DHC PLUS' ,'i') then GNN_steroid='DIHYDROCODEINE';
if find(description,'DIHYDROCODEINE', 'i') or find(description,'DIHYDROCODEIN-ACETAMINOPH-CAFF' ,'i') then GNN_steroid='DIHYDROCODEINE';
if find(description,'DIHYDROCODEINE', 'i') or find(description,'PANLOR DC' ,'i') then GNN_steroid='DIHYDROCODEINE';
if find(description,'DIHYDROCODEINE', 'i') or find(description,'PANLOR SS' ,'i') then GNN_steroid='DIHYDROCODEINE';
if find(description,'DIHYDROCODEINE', 'i') or find(description,'SINODEINE' ,'i') then GNN_steroid='DIHYDROCODEINE';
if find(description,'DIHYDROCODEINE', 'i') or find(description,'SYNALGOS-DC' ,'i') then GNN_steroid='DIHYDROCODEINE';
if find(description,'DIHYDROCODEINE', 'i') or find(description,'SYNGESIC DC' ,'i') then GNN_steroid='DIHYDROCODEINE';
if find(description,'DIHYDROCODEINE', 'i') or find(description,'SYNGESIC-DC' ,'i') then GNN_steroid='DIHYDROCODEINE';
if find(description,'DIHYDROCODEINE', 'i') or find(description,'TREZIX' ,'i') then GNN_steroid='DIHYDROCODEINE';
if find(description,'DIHYDROCODEINE', 'i') or find(description,'ZERLOR' ,'i') then GNN_steroid='DIHYDROCODEINE';
if find(description,'FENTANYL', 'i') or find(description,'DURAGESIC' ,'i') then GNN_steroid='FENTANYL';
if find(description,'FENTANYL', 'i') or find(description,'FENTANYL' ,'i') then GNN_steroid='FENTANYL';
if find(description,'FENTANYL', 'i') or find(description,'IONSYS' ,'i') then GNN_steroid='FENTANYL';
if find(description,'HYDROCODONE', 'i') or find(description,'ADOL' ,'i') then GNN_steroid='HYDROCODONE';
if find(description,'HYDROCODONE', 'i') or find(description,'ALLAY' ,'i') then GNN_steroid='HYDROCODONE';
if find(description,'HYDROCODONE', 'i') or find(description,'ALOR' ,'i') then GNN_steroid='HYDROCODONE';
if find(description,'HYDROCODONE', 'i') or find(description,'AMACODONE' ,'i') then GNN_steroid='HYDROCODONE';
if find(description,'HYDROCODONE', 'i') or find(description,'ANEXSIA' ,'i') then GNN_steroid='HYDROCODONE';
if find(description,'HYDROCODONE', 'i') or find(description,'ANODYNOS-DHC' ,'i') then GNN_steroid='HYDROCODONE';
if find(description,'HYDROCODONE', 'i') or find(description,'ANOLOR DH-5' ,'i') then GNN_steroid='HYDROCODONE';
if find(description,'HYDROCODONE', 'i') or find(description,'AZDONE' ,'i') then GNN_steroid='HYDROCODONE';
if find(description,'HYDROCODONE', 'i') or find(description,'BANCAP HC' ,'i') then GNN_steroid='HYDROCODONE';
if find(description,'HYDROCODONE', 'i') or find(description,'CETA-PLUS' ,'i') then GNN_steroid='HYDROCODONE';
if find(description,'HYDROCODONE', 'i') or find(description,'CO-GESIC' ,'i') then GNN_steroid='HYDROCODONE';
if find(description,'HYDROCODONE', 'i') or find(description,'DAMASON-P' ,'i') then GNN_steroid='HYDROCODONE';
if find(description,'HYDROCODONE', 'i') or find(description,'DOLACET' ,'i') then GNN_steroid='HYDROCODONE';
if find(description,'HYDROCODONE', 'i') or find(description,'DOLAGESIC' ,'i') then GNN_steroid='HYDROCODONE';
if find(description,'HYDROCODONE', 'i') or find(description,'DOLFEN' ,'i') then GNN_steroid='HYDROCODONE';
if find(description,'HYDROCODONE', 'i') or find(description,'DOLO PAP' ,'i') then GNN_steroid='HYDROCODONE';
if find(description,'HYDROCODONE', 'i') or find(description,'DOLO-PAP' ,'i') then GNN_steroid='HYDROCODONE';
if find(description,'HYDROCODONE', 'i') or find(description,'DOLOREX FORTE' ,'i') then GNN_steroid='HYDROCODONE';
if find(description,'HYDROCODONE', 'i') or find(description,'DOLPHEN' ,'i') then GNN_steroid='HYDROCODONE';
if find(description,'HYDROCODONE', 'i') or find(description,'DUOCET' ,'i') then GNN_steroid='HYDROCODONE';
if find(description,'HYDROCODONE', 'i') or find(description,'DURADYNE DHC' ,'i') then GNN_steroid='HYDROCODONE';
if find(description,'HYDROCODONE', 'i') or find(description,'GESIC-5' ,'i') then GNN_steroid='HYDROCODONE';
if find(description,'HYDROCODONE', 'i') or find(description,'GESIC-HD' ,'i') then GNN_steroid='HYDROCODONE';
if find(description,'HYDROCODONE', 'i') or find(description,'HBA' ,'i') then GNN_steroid='HYDROCODONE';
if find(description,'HYDROCODONE', 'i') or find(description,'HY-5' ,'i') then GNN_steroid='HYDROCODONE';
if find(description,'HYDROCODONE', 'i') or find(description,'HY-PHEN' ,'i') then GNN_steroid='HYDROCODONE';
if find(description,'HYDROCODONE', 'i') or find(description,'HYCET' ,'i') then GNN_steroid='HYDROCODONE';
if find(description,'HYDROCODONE', 'i') or find(description,'HYCO-PAP' ,'i') then GNN_steroid='HYDROCODONE';
if find(description,'HYDROCODONE', 'i') or find(description,'HYCODAPHEN' ,'i') then GNN_steroid='HYDROCODONE';
if find(description,'HYDROCODONE', 'i') or find(description,'HYCOGESIC' ,'i') then GNN_steroid='HYDROCODONE';
if find(description,'HYDROCODONE', 'i') or find(description,'HYCOMED' ,'i') then GNN_steroid='HYDROCODONE';
if find(description,'HYDROCODONE', 'i') or find(description,'HYCOTAB' ,'i') then GNN_steroid='HYDROCODONE';
if find(description,'HYDROCODONE', 'i') or find(description,'HYDROCET' ,'i') then GNN_steroid='HYDROCODONE';
if find(description,'HYDROCODONE', 'i') or find(description,'HYDROGESIC' ,'i') then GNN_steroid='HYDROCODONE';
if find(description,'HYDROCODONE', 'i') or find(description,'HYSINGLA ER' ,'i') then GNN_steroid='HYDROCODONE';
if find(description,'HYDROCODONE', 'i') or find(description,'IBUDONE' ,'i') then GNN_steroid='HYDROCODONE';
if find(description,'HYDROCODONE', 'i') or find(description,'LIQUICET' ,'i') then GNN_steroid='HYDROCODONE';
if find(description,'HYDROCODONE', 'i') or find(description,'LORCET' ,'i') then GNN_steroid='HYDROCODONE';
if find(description,'HYDROCODONE', 'i') or find(description,'LORCIDE' ,'i') then GNN_steroid='HYDROCODONE';
if find(description,'HYDROCODONE', 'i') or find(description,'LORTAB' ,'i') then GNN_steroid='HYDROCODONE';
if find(description,'HYDROCODONE', 'i') or find(description,'LORTAB ASA' ,'i') then GNN_steroid='HYDROCODONE';
if find(description,'HYDROCODONE', 'i') or find(description,'MARGESIC H' ,'i') then GNN_steroid='HYDROCODONE';
if find(description,'HYDROCODONE', 'i') or find(description,'MAXIDONE' ,'i') then GNN_steroid='HYDROCODONE';
if find(description,'HYDROCODONE', 'i') or find(description,'MEDICAP HD' ,'i') then GNN_steroid='HYDROCODONE';
if find(description,'HYDROCODONE', 'i') or find(description,'MEDIPAIN 5' ,'i') then GNN_steroid='HYDROCODONE';
if find(description,'HYDROCODONE', 'i') or find(description,'MEGAGESIC' ,'i') then GNN_steroid='HYDROCODONE';
if find(description,'HYDROCODONE', 'i') or find(description,'MEGAMOR' ,'i') then GNN_steroid='HYDROCODONE';
if find(description,'HYDROCODONE', 'i') or find(description,'MI-CODE' ,'i') then GNN_steroid='HYDROCODONE';
if find(description,'HYDROCODONE', 'i') or find(description,'NORCET' ,'i') then GNN_steroid='HYDROCODONE';
if find(description,'HYDROCODONE', 'i') or find(description,'NORCO' ,'i') then GNN_steroid='HYDROCODONE';
if find(description,'HYDROCODONE', 'i') or find(description,'ONCET' ,'i') then GNN_steroid='HYDROCODONE';
if find(description,'HYDROCODONE', 'i') or find(description,'ONCET 5' ,'i') then GNN_steroid='HYDROCODONE';
if find(description,'HYDROCODONE', 'i') or find(description,'ONCET 7' ,'i') then GNN_steroid='HYDROCODONE';
if find(description,'HYDROCODONE', 'i') or find(description,'PANACET' ,'i') then GNN_steroid='HYDROCODONE';
if find(description,'HYDROCODONE', 'i') or find(description,'PANASAL' ,'i') then GNN_steroid='HYDROCODONE';
if find(description,'HYDROCODONE', 'i') or find(description,'PANLOR' ,'i') then GNN_steroid='HYDROCODONE';
if find(description,'HYDROCODONE', 'i') or find(description,'POLYGESIC' ,'i') then GNN_steroid='HYDROCODONE';
if find(description,'HYDROCODONE', 'i') or find(description,'PROCET' ,'i') then GNN_steroid='HYDROCODONE';
if find(description,'HYDROCODONE', 'i') or find(description,'PROPAIN HC' ,'i') then GNN_steroid='HYDROCODONE';
if find(description,'HYDROCODONE', 'i') or find(description,'REPREXAIN' ,'i') then GNN_steroid='HYDROCODONE';
if find(description,'HYDROCODONE', 'i') or find(description,'ROGESIC #3' ,'i') then GNN_steroid='HYDROCODONE';
if find(description,'HYDROCODONE', 'i') or find(description,'SENEFEN III' ,'i') then GNN_steroid='HYDROCODONE';
if find(description,'HYDROCODONE', 'i') or find(description,'STAGESIC' ,'i') then GNN_steroid='HYDROCODONE';
if find(description,'HYDROCODONE', 'i') or find(description,'STAGESIC-10' ,'i') then GNN_steroid='HYDROCODONE';
if find(description,'HYDROCODONE', 'i') or find(description,'T-GESIC' ,'i') then GNN_steroid='HYDROCODONE';
if find(description,'HYDROCODONE', 'i') or find(description,'T-GESIC FORTE' ,'i') then GNN_steroid='HYDROCODONE';
if find(description,'HYDROCODONE', 'i') or find(description,'THERACODOPHEN-325' ,'i') then GNN_steroid='HYDROCODONE';
if find(description,'HYDROCODONE', 'i') or find(description,'THERACODOPHEN-650' ,'i') then GNN_steroid='HYDROCODONE';
if find(description,'HYDROCODONE', 'i') or find(description,'THERACODOPHEN-LOW-90' ,'i') then GNN_steroid='HYDROCODONE';
if find(description,'HYDROCODONE', 'i') or find(description,'UGESIC' ,'i') then GNN_steroid='HYDROCODONE';
if find(description,'HYDROCODONE', 'i') or find(description,'ULTRAGESIC' ,'i') then GNN_steroid='HYDROCODONE';
if find(description,'HYDROCODONE', 'i') or find(description,'VANACET' ,'i') then GNN_steroid='HYDROCODONE';
if find(description,'HYDROCODONE', 'i') or find(description,'VENDONE' ,'i') then GNN_steroid='HYDROCODONE';
if find(description,'HYDROCODONE', 'i') or find(description,'VERDROCET' ,'i') then GNN_steroid='HYDROCODONE';
if find(description,'HYDROCODONE', 'i') or find(description,'VICODIN' ,'i') then GNN_steroid='HYDROCODONE';
if find(description,'HYDROCODONE', 'i') or find(description,'VICODIN ES' ,'i') then GNN_steroid='HYDROCODONE';
if find(description,'HYDROCODONE', 'i') or find(description,'VICODIN HP' ,'i') then GNN_steroid='HYDROCODONE';
if find(description,'HYDROCODONE', 'i') or find(description,'VICOPROFEN' ,'i') then GNN_steroid='HYDROCODONE';
if find(description,'HYDROCODONE', 'i') or find(description,'XODOL 10-300' ,'i') then GNN_steroid='HYDROCODONE';
if find(description,'HYDROCODONE', 'i') or find(description,'XODOL 10/300' ,'i') then GNN_steroid='HYDROCODONE';
if find(description,'HYDROCODONE', 'i') or find(description,'XODOL 5-300' ,'i') then GNN_steroid='HYDROCODONE';
if find(description,'HYDROCODONE', 'i') or find(description,'XODOL 7.5-300' ,'i') then GNN_steroid='HYDROCODONE';
if find(description,'HYDROCODONE', 'i') or find(description,'XYLON 10' ,'i') then GNN_steroid='HYDROCODONE';
if find(description,'HYDROCODONE', 'i') or find(description,'ZAMICET' ,'i') then GNN_steroid='HYDROCODONE';
if find(description,'HYDROCODONE', 'i') or find(description,'ZOHYDRO ER' ,'i') then GNN_steroid='HYDROCODONE';
if find(description,'HYDROCODONE', 'i') or find(description,'ZOLVIT' ,'i') then GNN_steroid='HYDROCODONE';
if find(description,'HYDROCODONE', 'i') or find(description,'ZYDONE' ,'i') then GNN_steroid='HYDROCODONE';
if find(description,'HYDROMORPHONE', 'i') or find(description,'DILAUDID' ,'i') then GNN_steroid='HYDROMORPHONE';
if find(description,'HYDROMORPHONE', 'i') or find(description,'DILAUDID STRIP PACKS' ,'i') then GNN_steroid='HYDROMORPHONE';
if find(description,'HYDROMORPHONE', 'i') or find(description,'DILAUDID-5' ,'i') then GNN_steroid='HYDROMORPHONE';
if find(description,'HYDROMORPHONE', 'i') or find(description,'EXALGO' ,'i') then GNN_steroid='HYDROMORPHONE';
if find(description,'HYDROMORPHONE', 'i') or find(description,'HYDROMORPHONE ER' ,'i') then GNN_steroid='HYDROMORPHONE';
if find(description,'HYDROMORPHONE', 'i') or find(description,'HYDROMORPHONE HCL' ,'i') then GNN_steroid='HYDROMORPHONE';
if find(description,'HYDROMORPHONE', 'i') or find(description,'HYDROSTAT' ,'i') then GNN_steroid='HYDROMORPHONE';
if find(description,'HYDROMORPHONE', 'i') or find(description,'PALLADONE' ,'i') then GNN_steroid='HYDROMORPHONE';
if find(description,'LEVOMETHADYL', 'i') or find(description,'ORLAAM' ,'i') then GNN_steroid='LEVOMETHADYL';
if find(description,'LEVORPHANOL', 'i') or find(description,'LEVO-DROMORAN' ,'i') then GNN_steroid='LEVORPHANOL';
if find(description,'LEVORPHANOL', 'i') or find(description,'LEVORPHANOL TARTRATE' ,'i') then GNN_steroid='LEVORPHANOL';
if find(description,'MEPERIDINE', 'i') or find(description,'DEMEROL' ,'i') then GNN_steroid='MEPERIDINE';
if find(description,'MEPERIDINE', 'i') or find(description,'MEPERGAN FORTIS' ,'i') then GNN_steroid='MEPERIDINE';
if find(description,'MEPERIDINE', 'i') or find(description,'MEPERIDINE HCL' ,'i') then GNN_steroid='MEPERIDINE';
if find(description,'MEPERIDINE', 'i') or find(description,'MEPERIDINE W/PROMETHAZINE' ,'i') then GNN_steroid='MEPERIDINE';
if find(description,'MEPERIDINE', 'i') or find(description,'MEPERIDINE-PROMETHAZINE' ,'i') then GNN_steroid='MEPERIDINE';
if find(description,'MEPERIDINE', 'i') or find(description,'MEPERITAB' ,'i') then GNN_steroid='MEPERIDINE';
if find(description,'MEPERIDINE', 'i') or find(description,'MEPROZINE' ,'i') then GNN_steroid='MEPERIDINE';
if find(description,'METHADONE', 'i') or find(description,'DISKETS' ,'i') then GNN_steroid='METHADONE';
if find(description,'METHADONE', 'i') or find(description,'DOLOPHINE HCL' ,'i') then GNN_steroid='METHADONE';
if find(description,'METHADONE', 'i') or find(description,'METHADONE' ,'i') then GNN_steroid='METHADONE';
if find(description,'METHADONE', 'i') or find(description,'METHADONE HCL' ,'i') then GNN_steroid='METHADONE';
if find(description,'METHADONE', 'i') or find(description,'METHADONE INTENSOL' ,'i') then GNN_steroid='METHADONE';
if find(description,'METHADONE', 'i') or find(description,'METHADOSE' ,'i') then GNN_steroid='METHADONE';
if find(description,'MORPHINE', 'i') or find(description,'AVINZA' ,'i') then GNN_steroid='MORPHINE';
if find(description,'MORPHINE', 'i') or find(description,'EMBEDA' ,'i') then GNN_steroid='MORPHINE';
if find(description,'MORPHINE', 'i') or find(description,'KADIAN' ,'i') then GNN_steroid='MORPHINE';
if find(description,'MORPHINE', 'i') or find(description,'MORPHINE SULFATE' ,'i') then GNN_steroid='MORPHINE';
if find(description,'MORPHINE', 'i') or find(description,'MORPHINE SULFATE CONCENTRATE' ,'i') then GNN_steroid='MORPHINE';
if find(description,'MORPHINE', 'i') or find(description,'MORPHINE SULFATE CR' ,'i') then GNN_steroid='MORPHINE';
if find(description,'MORPHINE', 'i') or find(description,'MORPHINE SULFATE ER' ,'i') then GNN_steroid='MORPHINE';
if find(description,'MORPHINE', 'i') or find(description,'MS CONTIN' ,'i') then GNN_steroid='MORPHINE';
if find(description,'MORPHINE', 'i') or find(description,'MS/L' ,'i') then GNN_steroid='MORPHINE';
if find(description,'MORPHINE', 'i') or find(description,'MSIR' ,'i') then GNN_steroid='MORPHINE';
if find(description,'MORPHINE', 'i') or find(description,'OMS' ,'i') then GNN_steroid='MORPHINE';
if find(description,'MORPHINE', 'i') or find(description,'ORAMORPH SR' ,'i') then GNN_steroid='MORPHINE';
if find(description,'MORPHINE', 'i') or find(description,'RESCUDOSE' ,'i') then GNN_steroid='MORPHINE';
if find(description,'MORPHINE', 'i') or find(description,'ROXANOL' ,'i') then GNN_steroid='MORPHINE';
if find(description,'MORPHINE', 'i') or find(description,'ROXANOL SR' ,'i') then GNN_steroid='MORPHINE';
if find(description,'MORPHINE', 'i') or find(description,'ROXANOL UD' ,'i') then GNN_steroid='MORPHINE';
if find(description,'MORPHINE', 'i') or find(description,'ROXANOL-T' ,'i') then GNN_steroid='MORPHINE';
if find(description,'OXYCODONE', 'i') or find(description,'ALCET' ,'i') then GNN_steroid='OXYCODONE';
if find(description,'OXYCODONE', 'i') or find(description,'COMBUNOX' ,'i') then GNN_steroid='OXYCODONE';
if find(description,'OXYCODONE', 'i') or find(description,'DAZIDOX' ,'i') then GNN_steroid='OXYCODONE';
if find(description,'OXYCODONE', 'i') or find(description,'ENDOCET' ,'i') then GNN_steroid='OXYCODONE';
if find(description,'OXYCODONE', 'i') or find(description,'ENDOCODONE' ,'i') then GNN_steroid='OXYCODONE';
if find(description,'OXYCODONE', 'i') or find(description,'ENDODAN' ,'i') then GNN_steroid='OXYCODONE';
if find(description,'OXYCODONE', 'i') or find(description,'ETH-OXYDOSE' ,'i') then GNN_steroid='OXYCODONE';
if find(description,'OXYCODONE', 'i') or find(description,'LYNOX' ,'i') then GNN_steroid='OXYCODONE';
if find(description,'OXYCODONE', 'i') or find(description,'MAGNACET' ,'i') then GNN_steroid='OXYCODONE';
if find(description,'OXYCODONE', 'i') or find(description,'NARVOX' ,'i') then GNN_steroid='OXYCODONE';
if find(description,'OXYCODONE', 'i') or find(description,'OXAYDO' ,'i') then GNN_steroid='OXYCODONE';
if find(description,'OXYCODONE', 'i') or find(description,'OXECTA' ,'i') then GNN_steroid='OXYCODONE';
if find(description,'OXYCODONE', 'i') or find(description,'OXYCONTIN' ,'i') then GNN_steroid='OXYCODONE';
if find(description,'OXYCODONE', 'i') or find(description,'OXYFAST' ,'i') then GNN_steroid='OXYCODONE';
if find(description,'OXYCODONE', 'i') or find(description,'OXYIR' ,'i') then GNN_steroid='OXYCODONE';
if find(description,'OXYCODONE', 'i') or find(description,'PERCOCET' ,'i') then GNN_steroid='OXYCODONE';
if find(description,'OXYCODONE', 'i') or find(description,'PERCODAN' ,'i') then GNN_steroid='OXYCODONE';
if find(description,'OXYCODONE', 'i') or find(description,'PERCODAN DEMI' ,'i') then GNN_steroid='OXYCODONE';
if find(description,'OXYCODONE', 'i') or find(description,'PERCOLONE' ,'i') then GNN_steroid='OXYCODONE';
if find(description,'OXYCODONE', 'i') or find(description,'PERLOXX' ,'i') then GNN_steroid='OXYCODONE';
if find(description,'OXYCODONE', 'i') or find(description,'PRIMALEV' ,'i') then GNN_steroid='OXYCODONE';
if find(description,'OXYCODONE', 'i') or find(description,'PRIMLEV' ,'i') then GNN_steroid='OXYCODONE';
if find(description,'OXYCODONE', 'i') or find(description,'ROXICET' ,'i') then GNN_steroid='OXYCODONE';
if find(description,'OXYCODONE', 'i') or find(description,'ROXICODONE' ,'i') then GNN_steroid='OXYCODONE';
if find(description,'OXYCODONE', 'i') or find(description,'ROXICODONE INTENSOL' ,'i') then GNN_steroid='OXYCODONE';
if find(description,'OXYCODONE', 'i') or find(description,'ROXIPRIN' ,'i') then GNN_steroid='OXYCODONE';
if find(description,'OXYCODONE', 'i') or find(description,'TYLOX' ,'i') then GNN_steroid='OXYCODONE';
if find(description,'OXYCODONE', 'i') or find(description,'XARTEMIS XR' ,'i') then GNN_steroid='OXYCODONE';
if find(description,'OXYCODONE', 'i') or find(description,'XOLOX' ,'i') then GNN_steroid='OXYCODONE';
if find(description,'OXYCODONE', 'i') or find(description,'XTAMPZA ER' ,'i') then GNN_steroid='OXYCODONE';
if find(description,'OXYMORPHONE', 'i') or find(description,'OPANA' ,'i') then GNN_steroid='OXYMORPHONE';
if find(description,'OXYMORPHONE', 'i') or find(description,'OPANA ER' ,'i') then GNN_steroid='OXYMORPHONE';
if find(description,'OXYMORPHONE', 'i') or find(description,'OXYMORPHONE HCL' ,'i') then GNN_steroid='OXYMORPHONE';
if find(description,'OXYMORPHONE', 'i') or find(description,'OXYMORPHONE HCL ER' ,'i') then GNN_steroid='OXYMORPHONE';
if find(description,'PENTAZOCINE', 'i') or find(description,'PENTAZOCINE AND NALOXONE HCL' ,'i') then GNN_steroid='PENTAZOCINE';
if find(description,'PENTAZOCINE', 'i') or find(description,'PENTAZOCINE-ACETAMINOPHEN' ,'i') then GNN_steroid='PENTAZOCINE';
if find(description,'PENTAZOCINE', 'i') or find(description,'PENTAZOCINE-NALOXONE HCL' ,'i') then GNN_steroid='PENTAZOCINE';
if find(description,'PENTAZOCINE', 'i') or find(description,'TALACEN' ,'i') then GNN_steroid='PENTAZOCINE';
if find(description,'PENTAZOCINE', 'i') or find(description,'TALWIN COMPOUND' ,'i') then GNN_steroid='PENTAZOCINE';
if find(description,'PENTAZOCINE', 'i') or find(description,'TALWIN NX' ,'i') then GNN_steroid='PENTAZOCINE';
if find(description,'PROPOXYPHENE', 'i') or find(description,'BALACET 325' ,'i') then GNN_steroid='PROPOXYPHENE';
if find(description,'PROPOXYPHENE', 'i') or find(description,'BEXOPHENE' ,'i') then GNN_steroid='PROPOXYPHENE';
if find(description,'PROPOXYPHENE', 'i') or find(description,'DARVOCET A500' ,'i') then GNN_steroid='PROPOXYPHENE';
if find(description,'PROPOXYPHENE', 'i') or find(description,'DARVOCET-N 100' ,'i') then GNN_steroid='PROPOXYPHENE';
if find(description,'PROPOXYPHENE', 'i') or find(description,'DARVOCET-N 50' ,'i') then GNN_steroid='PROPOXYPHENE';
if find(description,'PROPOXYPHENE', 'i') or find(description,'DARVON' ,'i') then GNN_steroid='PROPOXYPHENE';
if find(description,'PROPOXYPHENE', 'i') or find(description,'DARVON COMPOUND' ,'i') then GNN_steroid='PROPOXYPHENE';
if find(description,'PROPOXYPHENE', 'i') or find(description,'DARVON COMPOUND 32' ,'i') then GNN_steroid='PROPOXYPHENE';
if find(description,'PROPOXYPHENE', 'i') or find(description,'DARVON COMPOUND-65' ,'i') then GNN_steroid='PROPOXYPHENE';
if find(description,'PROPOXYPHENE', 'i') or find(description,'DARVON-N' ,'i') then GNN_steroid='PROPOXYPHENE';
if find(description,'PROPOXYPHENE', 'i') or find(description,'DOLACET' ,'i') then GNN_steroid='PROPOXYPHENE';
if find(description,'PROPOXYPHENE', 'i') or find(description,'DOLAGESIC' ,'i') then GNN_steroid='PROPOXYPHENE';
if find(description,'PROPOXYPHENE', 'i') or find(description,'DOLENE' ,'i') then GNN_steroid='PROPOXYPHENE';
if find(description,'PROPOXYPHENE', 'i') or find(description,'DOLENE COMPOUND-65' ,'i') then GNN_steroid='PROPOXYPHENE';
if find(description,'PROPOXYPHENE', 'i') or find(description,'DORAPHEN-65' ,'i') then GNN_steroid='PROPOXYPHENE';
if find(description,'PROPOXYPHENE', 'i') or find(description,'DOXAPAP N-100' ,'i') then GNN_steroid='PROPOXYPHENE';
if find(description,'PROPOXYPHENE', 'i') or find(description,'DOXAPHENE COMPOUND' ,'i') then GNN_steroid='PROPOXYPHENE';
if find(description,'PROPOXYPHENE', 'i') or find(description,'E-LOR' ,'i') then GNN_steroid='PROPOXYPHENE';
if find(description,'PROPOXYPHENE', 'i') or find(description,'GENAGESIC' ,'i') then GNN_steroid='PROPOXYPHENE';
if find(description,'PROPOXYPHENE', 'i') or find(description,'J-CET' ,'i') then GNN_steroid='PROPOXYPHENE';
if find(description,'PROPOXYPHENE', 'i') or find(description,'LORCET' ,'i') then GNN_steroid='PROPOXYPHENE';
if find(description,'PROPOXYPHENE', 'i') or find(description,'MARGESIC' ,'i') then GNN_steroid='PROPOXYPHENE';
if find(description,'PROPOXYPHENE', 'i') or find(description,'MARGESIC COMPOUND' ,'i') then GNN_steroid='PROPOXYPHENE';
if find(description,'PROPOXYPHENE', 'i') or find(description,'PANCET' ,'i') then GNN_steroid='PROPOXYPHENE';
if find(description,'PROPOXYPHENE', 'i') or find(description,'PC-CAP' ,'i') then GNN_steroid='PROPOXYPHENE';
if find(description,'PROPOXYPHENE', 'i') or find(description,'PNAP' ,'i') then GNN_steroid='PROPOXYPHENE';
if find(description,'PROPOXYPHENE', 'i') or find(description,'PP-CAP' ,'i') then GNN_steroid='PROPOXYPHENE';
if find(description,'PROPOXYPHENE', 'i') or find(description,'PRO POX 65' ,'i') then GNN_steroid='PROPOXYPHENE';
if find(description,'PROPOXYPHENE', 'i') or find(description,'PROPACET' ,'i') then GNN_steroid='PROPOXYPHENE';
if find(description,'PROPOXYPHENE', 'i') or find(description,'PROPOXACET-N 100' ,'i') then GNN_steroid='PROPOXYPHENE';
if find(description,'PROPOXYPHENE', 'i') or find(description,'RO-CET-N 100' ,'i') then GNN_steroid='PROPOXYPHENE';
if find(description,'PROPOXYPHENE', 'i') or find(description,'SK-65' ,'i') then GNN_steroid='PROPOXYPHENE';
if find(description,'PROPOXYPHENE', 'i') or find(description,'TRYCET' ,'i') then GNN_steroid='PROPOXYPHENE';
if find(description,'PROPOXYPHENE', 'i') or find(description,'WYGESIC' ,'i') then GNN_steroid='PROPOXYPHENE';
if find(description,'TAPENTADOL', 'i') or find(description,'NUCYNTA' ,'i') then GNN_steroid='TAPENTADOL';
if find(description,'TRAMADOL', 'i') or find(description,'CONZIP' ,'i') then GNN_steroid='TRAMADOL';
if find(description,'TRAMADOL', 'i') or find(description,'RYBIX ODT' ,'i') then GNN_steroid='TRAMADOL';
if find(description,'TRAMADOL', 'i') or find(description,'RYZOLT' ,'i') then GNN_steroid='TRAMADOL';
if find(description,'TRAMADOL', 'i') or find(description,'THERATRAMADOL' ,'i') then GNN_steroid='TRAMADOL';
if find(description,'TRAMADOL', 'i') or find(description,'TRAMADOL HCL-ACETAMINOPHEN' ,'i') then GNN_steroid='TRAMADOL';
if find(description,'TRAMADOL', 'i') or find(description,'ULTRACET' ,'i') then GNN_steroid='TRAMADOL';
if find(description,'TRAMADOL', 'i') or find(description,'ULTRAM' ,'i') then GNN_steroid='TRAMADOL';
